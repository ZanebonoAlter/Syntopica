[project]
name = "acceptance-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "playwright>=1.40",
    "requests>=2.31",
]

[tool.pytest.ini_options]
testpaths = ["changes"]
pythonpath = ["."]
